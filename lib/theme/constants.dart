import 'package:flutter/material.dart';

const Color kPrimaryColor = Color(0xff);
const Color kSecondryColor = Color(0xff);
const Color kPrimaryTextColor = Color(0xff);
const Color kSecondryTextColor = Color(0xff);

const Duration kAnimationDuration = Duration(microseconds: 250);
